---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: quobyte-meta
spec:
  selector:
    matchLabels:
      app: quobyte-meta  # has to match .spec.template.metadata.labels
  serviceName: "quobyte-meta"
  replicas: {{ .Values.quobyte.replicas.metadata }}
  podManagementPolicy: Parallel
  template:
    metadata:
      labels:
        app: quobyte-meta  # has to match .spec.selector.matchLabels
      annotations:
        timestamp: "{{ now }}"
    spec:
      terminationGracePeriodSeconds: 10
      hostNetwork: {{ .Values.hostNetwork }}
      dnsPolicy: ClusterFirstWithHostNet
      tolerations:
        - key: {{ .Values.taintKey }}
          operator: "Exists"
          {{- if .Values.taintValue }}
          value: {{ .Values.taintValue | quote }}
          {{- end }}
          effect: "NoSchedule"
      containers:
        - name: quobyte
          image: {{ .Values.quobyte.image }}
          imagePullPolicy: Always
          securityContext:
            privileged: true
          env:
            - name: NODENAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: MY_POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: HOST_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: QUOBYTE_DATA_PORT
              value: "7863"
            - name: QUOBYTE_META_PORT
              value: "7862"
            - name: QUOBYTE_REG_PORT
              value: "7861"
            - name: QUOBYTE_API_PORT
              value: "7860"
            - name: QUOBYTE_S3_PORT
              value: "8484"
            - name: CLOUD_INSTALL
              value: "1"
            - name: QUOBYTE_SERVICE
              value: metadata
            - name: QUOBYTE_REGISTRY
              value: _quobyte._tcp.quobyte.{{ .Release.Namespace }}.{{ .Values.clusterDnsSuffix }}
            - name: CLUSTER_NS
              value: {{ .Release.Namespace }}
            - name: LIMIT_OPEN_FILES
              value: "{{ .Values.quobyte.maxopenfiles }}"
            - name: LIMIT_MAX_PROCESSES
              value: "{{ .Values.quobyte.maxprocs }}"
            {{- if .Values.quobyte.enable_rdma.metadata }}
            - name: QUOBYTE_ENABLE_RDMA
              value: "{{ .Values.quobyte.enable_rdma.metadata }}"
            {{- end }}
            {{- if .Values.quobyte.memory.maxmem.metadata }}
            - name: QUOBYTE_MAX_MEM_METADATA
              value: "{{ .Values.quobyte.memory.maxmem.metadata }}"
            {{- end}}
            {{- if .Values.quobyte.memory.maxbuffer.metadata }}
            - name: QUOBYTE_MAX_BUFFER_METADATA
              value: "{{ .Values.quobyte.memory.maxbuffer.metadata }}"
            {{- end}}
          # work around check in /opt/main
          volumeMounts:
            - name: workaround
              mountPath: /devices
            - name: dev-disk
              mountPath: /dev/disk
            - name: host-udev
              mountPath: /run/udev
          ports:
            - containerPort: 7862
              name: quobyte-meta
          {{- with .Values.quobyte.resources.metadata }}
          resources:
            {{- $cpuReq := "" }}
            {{- $cpuLim := "" }}
            {{- $memReq := "" }}
            {{- $memLim := "" }}
            {{- if .cpu }}
              {{- if .cpu.requests }}{{- $cpuReq = (.cpu.requests | toString) }}{{- end }}
              {{- if .cpu.limit }}{{- $cpuLim = (.cpu.limit | toString) }}{{- end }}
            {{- end }}
            {{- if .memory }}
              {{- if .memory.requests }}{{- $memReq = (.memory.requests | toString) }}{{- end }}
              {{- if .memory.limit }}{{- $memLim = (.memory.limit | toString) }}{{- end }}
            {{- end }}
            {{- if or $cpuReq $memReq }}
            requests:
              {{- if $cpuReq }}
              cpu: {{ $cpuReq | quote }}
              {{- end }}
              {{- if $memReq }}
              memory: {{ $memReq | quote }}
              {{- end }}
            {{- end }}
            {{- if or $cpuLim $memLim }}
            limits:
              {{- if $cpuLim }}
              cpu: {{ $cpuLim | quote }}
              {{- end }}
              {{- if $memLim }}
              memory: {{ $memLim | quote }}
              {{- end }}
            {{- end }}
          {{- end }}

      volumes:
        - hostPath:
            # work around check in /opt/main
            path: /tmp
            type: ""
          name: workaround
        - hostPath:
            path: /dev/disk
            type: ""
          name: dev-disk
        - hostPath:
            path: /run/udev
            type: ""
          name: host-udev


      {{- if .Values.nodepoolname }}
      nodeSelector:
         storage-node-pool: {{ .Values.nodepoolname }}
      {{- end }}
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - quobyte
              topologyKey: "kubernetes.io/hostname"
